import { styled } from "@stitches/react";

export const Container = styled('main', {
  display: 'flex',
  flexDirection: 'column',
  gap: '2rem',
  width: '100%',
  maxWidth: 1180,
  margin: '0 auto',
  marginTop: '6rem',
})